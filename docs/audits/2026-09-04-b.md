# Audit #2 (scoped) — 2026-09-04 — re-verification of newly closed issues #019 #020 #023

Auditor: separate adversarial session (scoped follow-up to docs/audits/2026-09-04.md;
prior findings NOT re-litigated). Commit audited: b152bdc (head of main).
Covered merges: cc992e1 (#019 leaves), 1db5139 (#023 save/load), b152bdc (#020 light,
with #028 torch carve-out issue filed). Working tree clean before and after all gate runs
(`git status` empty; parity-latest.json regenerated byte-identical; build deterministic).

## Final gate (reproduced verbatim)

`node tools/build.mjs` → `build OK -> game/index.html (91.9 KB, atlas embedded)` exit 0.
`node tools/test.mjs` → `TEST GREEN`, **63/63 asserts, 0 fails, 0 console errors**,
ticks: 33, registry blocks: 17. Families relevant to this audit:
- world.leaves-decay(0), world.leaves-persist(63), interact.leaves-drop(28/600)
- light.source(15), light.falloff(11,7), light.occluded(0), light.sky(15),
  light.queue(stale ok), light.queue-off(15->0)
- save.rle-unit, save.size(16810B), save.load, save.edits(5), save.seed, save.player,
  save.corrupt(bad=false,rawIn={corrupt!,rej=parse)

## Parity line

`node tools/parity.mjs` →
```
PARITY @ 2026-09-04 — functional variants 16/399 (4.0%)
  tier 1: 16/125   tier 2: 0/163   tier 3: 0/111
```
Expected 16/399 with glowstone as the new count (audit #1's honest 15 + glowstone).
The 15 unproven registered variants are listed honestly as "not counted".

## Task 1 — #019 leaves: PARTIAL

- Code is real and matches claims: `decayLeavesNear` (src/world.js:110-126) = Chebyshev
  radius-6 log check triggered only on log removal (src/world.js:104); drop branch
  (src/interact.js:75-78): roll<5% sapling, <5.5% apple(=0.5%), else nothing.
- Asserts exist & green: world.leaves-decay(0) all-leaves-decay after chopping,
  world.leaves-persist(63) intact-tree control, interact.leaves-drop(28/600)=4.7%
  inside a 2%–9.2% statistical band around 5% (src/interact.js:155-166, 600 real breaks).
- Shot qa/2026-09-04/leaf-decay.png exists (94.9 KB, forest w/ felled gap per evidence).
- **Is 5%/0.5% defensible for 1.12.2? YES** — Java 1.12.2 oak leaves: 5% sapling,
  0.5% apple (1/200), plus 2% stick. The two shipped numbers are correct. BUT:
  1. The issue body spec still carries the unresolved markers "[verify 1.12 exact:
     0.275 sapling? apple: 0.5%/0.5%/2.5% by luck? research]" (issues/019:4-5) while
     the close evidence claims "1.12 loot chances **resolved in issue body**" — false.
     DoR rule "[TBC] remains → cannot be READY" was violated again (audit #1 F2 class).
  2. REFERENCE.md:43 still says "decay radius 6 **[TBC]**" and lists no drop numbers —
     #011 AC1 ("[TBC] removed" from leaves row) was closed incomplete; issue #011 body
     still says "believed ~10% sapling", contradicting the shipped 5%.
  3. Stick drop (2%) missing from the branch.
  4. Fidelity simplifications (recorded, not blocking): vanilla decay is
     connectivity/path-distance + air-touch + random-tick; clone is straight-line
     Chebyshev-6, instant on log removal. Vanilla apple also drops from dark oak only
     alongside oak (clone has a single oak-like leaves type — acceptable at 14/399 scale).
→ P1 finding F1, FIX issue 029 opened (spec honesty + stick drop; values themselves kept).

## Task 2 — #020 light: PASS

- 5 of the 6 "light asserts" are genuine behavior asserts (src/world.js:311-340):
  source cell =15, falloff 11@4 and 7@8 (exact linear −1/block), occluded =0 behind a
  1-thick 19-block stone wall, open-column sky =15, and a REAL budget test: source
  removed, relight must bring light to 0 within 10 budgeted ticks (queue-off 15->0).
- Built artifact: glowstone entry in game/index.html:524-557 carries
  functional:true, light:15, proof.issue "#020" and ALL SIX proof test names
  (light.source/falloff/occluded/sky/queue-off + interact.place) — each appears
  literally and each is green in the run. Count is honestly proof-bound.
- Vision: qa/2026-09-04/glow-cave.png inspected — radial falloff is REAL, not flat:
  floor bright near the glowstone, gradient darkening over ~8 cells, unlit carved-wall
  rock and outside stone fall to near-black. AC4 vision PASS.
- Baselines: starter-world.png + all qa/blocks sheets refreshed inside merge b152bdc
  and the refresh is explicitly documented in the issue (issues/020:19, "intentional
  visual change: light-bucket greedy splits + framing") — exactly the disclosure that
  was requested. Legit.
- Nits: `light.queue(stale ok)` is a VACUOUS assert — `CF.assert(r,...,true)` at
  src/world.js:337 (placeholder left in the suite; budget is only truly proven by the
  following queue-off) → F2 P2. AC4 text says shot "torch-night" but the shipped shot
  is glow-cave (torch carved to #028, which exists) → F3 P3. registry "tier":2 vs
  catalog tier 1 for glowstone → F4 P3. occluded assert threshold `<=2` is looser than
  the evidence's "(0)" (run value was 0, honest) → F5 P3.

## Task 3 — #023 save/load: PASS

- 7 save asserts green incl. the honest corrupt-fallback (bad=false, world usable,
  no throw) and tower-survives-wrong-seed-reload (save.edits(5), seed swap to 999999
  proves edits came from the save, not the seed).
- Vision: qa/2026-09-04/tower-save.png shows the 5-block cobblestone tower at the
  test site after the saveNow()+loadNow() round-trip. PASS (tower is modest 1x1 but
  distinct against grass).
- `?new=1` wipe is correct-by-ordering (src/persist.js:110-111): the removeItem runs
  synchronously at persist.js eval, BEFORE the `setTimeout(loadNow,0)` auto-load;
  the beforeunload save fires on the OLD page's unload, so a reload with ?new=1
  always wipes after that write and before any load. Post-wipe autosave re-creates a
  save during the new session — intended ("start new, save fresh").
- Nits: spec says load "via ?load=1" but implementation auto-loads unconditionally
  (evidence discloses "auto-load on boot" — superset of spec) → F6 P3. AC4 promises
  size proof "for 40 edited chunks" but the assert only checks bytes<150KB &&
  chunks>=1 (measured 16810B) → F6 P3. Evidence "56 asserts GREEN" is stale (now 63) → F7 P3.

## Findings (severity per §9; P0 none)

| ID | Sev | One-liner |
|----|-----|-----------|
| F1 | P1 | #019 shipped counted drops from an unresolved "[verify/research]" spec; close evidence falsely claims body-resolution; REFERENCE leaves row still [TBC] + says "~10% sapling" vs shipped 5%; 2% stick drop missing → issue 029 |
| F2 | P2 | Vacuous assert `light.queue(stale ok)` = `CF.assert(...,true)` (src/world.js:337): inflates the "6 light asserts" claim; delete or make it assert a real stale-frame invariant |
| F3 | P3 | #020 AC4 names shot "torch-night"; shipped shot is glow-cave (torch deferred to #028) |
| F4 | P3 | glowstone registry `"tier": 2` vs catalog.json `tier: 1` — parity buckets by catalog; two sources disagree |
| F5 | P3 | light.occluded threshold `<=2` looser than evidence text "(0)" |
| F6 | P3 | #023 spec/impl divergences: auto-load unconditional vs spec's ?load=1; save.size assert doesn't check the "40 edited chunks" its AC promises |
| F7 | P3 | AGENTS.md memory stale/corrupt: "59 asserts" (now 63); line 53 fuses f3.js+world-light entries with literal `` `n `` instead of newlines; #023 evidence "56 asserts" stale |
| F8 | P3 | Leaves-decay fidelity: straight-line Chebyshev-6 instant decay vs vanilla connectivity-distance + air-touch + random-tick (simplification undocumented in REFERENCE) |

Positives (recorded): gate fully green & reproducible, tree clean throughout; proof
literals genuinely present in the built file; glow-cave falloff is real rendered
lighting (per-vertex, smooth) not a flat tint; baseline refresh pre-documented exactly
as the loop demands; save-corruption debug instrumentation (rawIn/raw diff) is good
engineering hygiene; ?new=1 ordering handles the beforeunload-save race correctly.

## Verdicts

- #019 leaves: **PARTIAL** — real decay+drops and green statistical asserts, but spec
  markers unresolved + false "resolved in body" claim + missing stick drop (issue 029).
- #020 light: **PASS** — genuine light engine, honest proof-bound count, visible radial
  falloff, baselines documented; one vacuous placeholder assert to remove (F2).
- #023 save/load: **PASS** — round-trip proven at harness + shot level, wipe ordering
  correct; two spec/AC wording divergences recorded as P3.

Re-check at next audit: 029 closed with REFERENCE [TBC]s actually removed;
`light.queue` vacuous assert gone from suite; AGENTS.md assert count current.
