# Audit #3 (scoped) — 2026-09-04 — #021 day/night + #024 items/tools/crafting + honesty/git hygiene

Auditor: separate adversarial session (scoped follow-up to docs/audits/2026-09-04-b.md;
prior findings NOT re-litigated). Commit audited: f5ef998 (head of main).
Covered merges: 497d5a3+36ffe2b (#021), 8555b73+f5ef998 (#024). Working tree clean
before and after all gate runs (`git status --porcelain` empty; parity-latest.json
byte-identical; build deterministic). Only temp artifact created during audit
(block.png probe shot) was deleted; tree clean at close.

## Final gate (reproduced verbatim)

`node tools/build.mjs` → `build OK -> game/index.html (118.9 KB, atlas embedded)` exit 0.
`node tools/test.mjs` → `TEST GREEN`, **80 asserts pass (0 fail, 0 console errors)**,
`  ticks: 33, registry blocks: 20`.
NOTE: the loop's expected "74 asserts" is stale — the true current count is 80
(AGENTS.md line 14 still says 74 → F3). Gate itself is green and reproducible.

## Parity line

```
PARITY @ 2026-09-04 — functional variants 17/399 (4.3%)
  tier 1: 17/125
  tier 2: 0/163
  tier 3: 0/111
```
17 = 16 (audit #2 state) + torch. The 17 registered-but-unproven variants (planks/logs/
leaves wood variants, bedrock, red_sand, crafting_table, furnace) are listed honestly
as "not counted". furnace + crafting_table are `functional: false` in the built
registry (verified by regex on game/index.html) — correctly NOT counted. ✔

## Task 1 — #021 day/night: PARTIAL (P2)

- Curve is real (src/world.js:296-302): day=1, cos-ease dusk 12000-13800, night=0
  13800-22800, cos-ease dawn 22800-24000. The 6 time asserts are genuine, not vacuous:
  time.day pins df(1000)===1, time.night pins df(18000)===0 (a constant-curve stub
  cannot pass both), dusk/dawn-monotonic sample every 180/120 ticks AND pin both
  endpoints (world.js:377-383). time.2days-no-crash runs 48k real world.tick().
- time.pixel-night is a real GPU A/B: same camera, `CF.timeOffset` toggled, readPixels
  center pixel, asserts nightL < dayL*0.6 && dayL > 100 (src/render.js:278-284);
  run value 69 < 157 — real rendered darkening (uDay uniform + sky/fog mix,
  render.js:232-241), not a tint hack.
- qa/2026-09-04/night-glow.png inspected: unambiguously night (black sky, dim moonlit
  grass) with a visible warm glowstone block + local illumination against the tree —
  AC3 vision PASS. F3 Time/Daylight readout exists (src/f3.js:25) as evidence claims.
- **FINDING: AC1 half-unproven.** AC1 is checked [x] and the evidence repeats it
  ("covered cave block skylight=0 at noon"), but NO cave sky-zero assert exists in the
  suite — only open-sky=15 (light.sky) and block-channel occlusion (light.occluded).
  The sky-column seeder (world.js:166-177) does zero under solid cover by construction,
  but per §7 "counts/asserts need evidence" the claim is currently vacuous → F2 (P2: no
  issue opened per the P0/P1-only rule; fold the light.sky-cave assert into the next sprint).
- Positive: audit #2's F2 nit fixed — the old vacuous `light.queue(stale ok)`
  (`CF.assert(...,true)`) was replaced by a real stale-frame invariant assert
  (world.js:346: light must still read >0 immediately after source removal, pre-relight).

## Task 2 — #024 items/tools: PARTIAL (P1 evidence defect)

- **Recipe accuracy vs Java 1.12.2: CORRECT on all spot-checks.** src/items.js:74-90:
  log→4 planks (shapeless) ✔; 2 planks vertical→4 sticks ✔; coal+stick→**4 torches** ✔
  (Java 1.12 torch output is 4 — the prompt's suspicion is unfounded);
  3 mat + 2 sticks→**1** pickaxe ✔; plus verified extras: crafting_table 2x2 planks ✔,
  furnace 8-cobble ring ✔, chest 8-plank ring ✔, shears 2-iron diagonal ✔, axe/shovel/
  sword patterns ✔, tool outputs n=1 ✔. Break-time model matches vanilla formula:
  stone/wood-pick 1.125s and stone/hand 7.5s are both vanilla-exact (speed/(hardness*1.5),
  x5 on can't-harvest). gold_ore minTier 3 → canHarvest wood=false/iron=true ✔ (1.12:
  gold ore needs iron). Smelt 200t/item + coal 1600t fuel ✔.
- items.gate-gold + items.speed-stone-wood literals are present in built
  game/index.html and — the real proof — run green FROM the built file via test.mjs.
- Torch proof-bound (task 4): registry torch functional:true with proof.tests
  [items.torch, items.torch-light, interact.place, light.source] — all four literals
  present in game/index.html (counts 4/2/17/3) and all green in the run. Honest.
- qa/2026-09-04/torch-craft.png inspected: night carved room, cross-model torch with
  bright flame head, radial light pool on the floor with visible stepped falloff to
  darkness at room corners — hero-shot claim holds (pool is light-bucket quantized,
  diamond-ish, but genuinely radial + distance-graded). Vision PASS.
- **FINDING: qa/blocks/torch.png (committed, used as parity evidence requirement #4)
  is a broken render** — a green-field landscape with a grid pattern of brown garbage
  quads, no pedestal, no torch, room entirely unmeshed. It captured the pre-fix
  cross-model const-assignment bug (the one #024's evidence says was found+fixed):
  the sheet was generated during the broken run and never regenerated after the fix.
  Reproduced on current build (`node tools/shot.mjs block block=torch seed=5`):
  subject renders correctly (torch cross + warm pool on pedestal) — so the code is
  fine, the committed evidence is stale garbage. Torch's counted status currently
  rests on an invalid sheet → F1 P1, issue 030 (regenerate + audit sibling sheets).
- Evidence miscount: close evidence claims "13 item asserts green"; the suite has 11
  items.* asserts → F3 (with AGENTS staleness).

## Task 4 — honesty scan

- Built registry: 17 functional:true entries = 17 counted (parity binds proof.tests
  against built-file literals — tools/parity.mjs:36-40, sound mechanism).
- furnace/crafting_table functional:false in built artifact → not counted ✔
  (GUI interaction deferred to #025, documented).
- qa/blocks/torch.png exists (parity existsSync passes) but see F1 — exists ≠ valid.
- 20 files tracked under spike/ — explicitly allowed scratch per AGENTS.md; nothing
  outside spike/ looks like scratch junk. git status clean before/after. ✔

## Findings (severity per §9; P0 none)

| ID | Sev | One-liner |
|----|-----|-----------|
| F1 | P1 | Committed qa/blocks/torch.png shows a broken pre-fix render (no torch/room, garbage quad grid) — the parity evidence for the only newly counted block; current build renders the subject correctly, sheet was never regenerated after the cross-model fix → issue 030 |
| F2 | P2 | #021 AC1 "covered cave skylight=0 at noon" marked [x] but no such assert exists anywhere in the suite (only open-sky=15 + block-light occlusion) — no issue (P0/P1-only rule); add light.sky-cave(0) next sprint |
| F3 | P3 | Count/memory staleness (audit #2 F7 recurrence): AGENTS.md says "74 asserts", true count 80; #024 evidence says "13 item asserts", suite has 11 items.*; AGENTS.md:53 still contains literal `` `n `` fused lines (unfixed from #2) |
| F4 | P3 | #021 evidence "Moonlight = sky level 4 equivalent (documented in render.js uniform floor)": floor is uDay=max(dayF,0.25) → 3.75/15 equivalent, and the "documented" comment lives in world.js:294, not render.js — minor claim inflation |
| F5 | P3 | #024 spec line promises tool speeds "…gold12" but items.js TOOLS has no gold tier/item at all (spec/impl divergence; no AC claims gold tools) |
| F6 | P3 | SMELT map carries dead key `raw: null` (harmless, junk) in src/items.js:151 |

Positives (recorded): all four spot-check recipes exactly 1.12 (incl. the torch x4 the
prompt suspected); break-time numbers vanilla-exact; time/pixel asserts genuinely
non-vacuous (endpoints + monotone sampling + GPU A/B); audit #2's vacuous queue assert
was actually fixed (F2 closed); proof literals for torch all present and green in the
built artifact; furnace/crafting_table honestly uncounted; SHOTERR probe (harness.js:48)
is good regression hygiene; gate deterministic and tree clean throughout.

## Verdicts

- #021 day/night: **PARTIAL** — real curve, real asserts, real night shot; AC1's
  cave-sky-zero is claimed but unasserted (F2).
- #024 items/tools: **PARTIAL** — recipe set 1.12-accurate and asserts real/proof-bound,
  but the committed torch evidence sheet is a stale broken render (F1 → issue 030).

Re-check at next audit: 030 closed with regenerated sheet + sibling-sheet check list;
a green light.sky-cave(0) assert present (F2); AGENTS assert count current;
`light.queue-stale` (real) stays.
