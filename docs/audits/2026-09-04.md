# Audit — 2026-09-04 (adversarial re-verification of main @ v0.1.0, MASTERPROMPT §9)

Auditor: separate adversarial session. Scope: full gate re-run, DoD evidence spot-check
(#003 #005 #006 #007 #009), metric-cheating hunt, §1.1 offline check, Scrum adherence,
REFERENCE honesty, parity-tamper experiments (temp edits to built artifact, reverted).
Commit audited: b58d4ae (tag v0.1.0 → b58d4ae confirmed via `git rev-parse v0.1.0`).

## Task 1 — Quality gate re-run (reproduced outputs, verbatim)

`node tools/build.mjs` →
```
build OK -> game/index.html (62.5 KB, atlas embedded)
```
exit 0. No tracked file changed by the build (working tree stayed clean).

`node tools/test.mjs` →
```
TEST GREEN
  pass: boot.ready, gl.webgl2, loop.ticks, registry.json, atlas.decoded,
  registry.tiles-exist, registry.tiles-6-faces, registry.planks-oak,
  registry.ids-unique, world.chunks, world.surface-solid, world.bedrock,
  world.deterministic, world.set-persists, world.dirty-marked,
  world.dirty-neighbor, world.ores-exist, world.gen-budget,
  render.mesh-all(700ms), render.px(163,164,165), render.atlas,
  render.merged(80/81), render.tris(30910,rebuilds=83), render.glErr,
  render.no-missing-tiles, render.dirty-fast, player.land, player.move(10.1),
  player.no-sink, player.gravity-fall, player.camera, interact.aim-hit,
  interact.break-time(1.12 dirt=0.75s, got 0.75), interact.drop, interact.air,
  interact.slow-tier, interact.slow-time(11.25s), interact.bedrock,
  interact.place, interact.no-self-place, f3.overlay-exists, f3.toggle
  ticks: 26, registry blocks: 17
```
exit 0, 41/41 pass, 0 fails, 0 console errors.

`node tools/parity.mjs` →
```
PARITY @ 2026-09-04 — functional variants 16/399 (4.0%)
  tier 1: 15/125
  tier 2: 1/163
  tier 3: 0/111
```
exit 0. Matches docs/PARITY.md table exactly. Independent catalog recount via
node on docs/catalog.json: 184 entries / 399 variants / tiers 125-163-111 — matches.

`node tools/shot.mjs walking seed=5` →
```
shot OK -> qa\2026-09-04\walking.png (61920 bytes)
```
exit 0; regenerated PNG byte-identical to qa/baseline/walking.png (git clean after)
— deterministic harness confirmed, no regression vs baseline.

Verdict: gate GREEN, honestly reproduced. Bonus: re-running tools/tex/gen.mjs
(Blender) regenerated atlas.png/json byte-identically — pipeline is reproducible.

## Task 2 — DoD evidence spot-check (#003 #005 #006 #007 #009)

Cited qa PNGs: all exist, non-trivial (40–183 KB). Built-artifact registry parsed
independently: exactly 16 variants carry `functional:true` (the 16 parity counts);
bedrock is `functional:false` in the built file — honest non-count. Built registry
literal is byte-identical to src/registry.js (marker extraction, both sides).

- #003 renderer: PASS. Evidence claims match reality (render.merged 80/81, mesh-all
  700ms, dirty-fast, glErr 0 asserts are real code in src/render.js with pixel-read
  + missing-tile-set checks; starter-world.png vision-verified: textured grass,
  trees, fog, zero magenta). Nit: AC4 text says <2s, harness threshold is 6000ms
  (measured 700ms, so AC actually met).
- #005 player: PASS with gaps. land/move(10.1)/gravity-fall/camera asserts real;
  walking.png is a real first-person render. Gaps: AC3 text says "+10 block fall",
  test falls from +12 (exceeds, fine); NO horizontal wall-collision assert exists
  anywhere behind the "AABB collision" claim (finding P2-5); step-up 0.55 ≠
  REFERENCE 0.6 (finding P2-4).
- #006 break/place: PARTIAL. asserts real (dirt 0.75s exact, stone-not-broken,
  bedrock ∞, place, no-self-place); qa/blocks sheets real. BUT AC2 text says
  "stone=2.25s" while Evidence says "11.25s (1.12)" — internally contradictory,
  and the true 1.12.2 value is 7.5s (finding P1-1). Gold ore drop implemented
  from a spec line still marked [TBC] (finding P1-2).
- #007 textures: PASS. atlas.json = 20 tiles, all src `blender:gen.py`; atlas_sheet.png
  vision-checked: grain visible on every tile, log_top rings present, glass interior
  empty w/ pale frame, leaves speckled — no flat-color tiles (§1.2 ok).
- #009 F3: asserts f3.overlay-exists + f3.toggle exist and pass; overlay code shows
  fps/XYZ/chunk/facing/target/chunks/tris/seed/hotbar/drops. BUT the issue file
  has NONE of the §3-required sections (no SMART, spec, ACs, test plan) yet is DONE
  (finding P2-3); F3 lacks REFERENCE-listed light/biome/memory/entity fields.

Parity gaming tests (temp edits to built game/index.html, both reverted via
`git checkout`; final `git status` clean):
- GAME A: flip bedrock `functional:false`→`true` in built file → parity printed
  17/399 (t1 16/125). The flag is self-declared; a counted block with tile+png is
  trusted with zero behavioral verification. → finding P2-1.
- GAME B: inject fake `planks:dark_oak functional:true` w/ blender tile, no png →
  parity stayed 16/399. The missing-png guard WORKS; flags alone cannot mint a
  block that lacks texture/evidence.

## Task 3 — Metric-cheating hunt

- 16/399 is faithful to parity.mjs's rules and the tool's rules resist flag-only
  gaming (GAME B), BUT: (a) **snow** is counted as the sole tier-2 point and is NOT
  a 1.12.2 registered variant (full snow cube = 1.13+; 1.12 has snow_layer 78, 8
  metas, silk-touch-only drop per REFERENCE — catalog itself contradicts REFERENCE).
  Auditor's corrected honest count: **15/399 (t2 0/163)** pending catalog fix (P1-3).
  (b) §7's definition ("its 1.12.2 behavior/interactions implemented") is not met by
  counted grass (no spread), sand/gravel (no gravity, no flint chance), leaves
  (no decay, drop:null vs REFERENCE sapling/apple) → 4 of 16 over-claimed (P1-4).
  (c) bedrock correctly NOT counted (creative-only, unbreakable; flag false; consistent
  with REFERENCE + §7) — no cheating found there.
- qa PNGs are real textured renders (inspected walking.png, blocks/grass.png,
  blocks/coal_ore.png, blocks/snow.png, atlas_sheet.png): correct per-face
  banding, noise grain, no magenta anywhere. Nit: grass sheet low contrast
  (issue #007 already logged the nit).
- PARITY.md [x] items: "[x] block place/break/AABB collision" — place/break/land
  asserts exist, but no horizontal-collision assert backs "collision" (P2-5).
  "[x] F3 debug" — backed by existence+toggle only, content unchecked (P2-3).
  Other Tier-1 unchecked items are honestly unchecked (incl. "hotbar select only",
  "tier GATES done; tools pending"). The 399-vs-~250 catalog discrepancy is
  openly recorded — good honesty culture.

## Task 4 — §1.1 offline constraint (built artifact)

grep of game/index.html for `http://`, `https://`, `fetch(`, `XMLHttpRequest`,
`import(`, `<script ... src=`, `<link`, `@import`, `url(`: **zero hits**. Atlas is
base64-inline (`window.__ATLAS_B64`). build.mjs lines 36–47 enforce this same audit
at build time. PASS.

## Task 5 — Scrum adherence

- docs/sprints/01.md exists; goal stated; committed table lists #002–#009 (all 8) + #010
  reserved for auditor. All 8 merged to main.
- git graph: every feature landed via `--no-ff` merge w/ conventional commit + issue ref
  (e.g. `feat(player): AABB physics + controls (#005)`). Deviation: #006+#008+#009 share
  ONE merge commit b58d4ae (per-issue flow broken for 2 of 3).
- VIOLATIONS: Sprint Review + Retro in 01.md are still "(fill at close)" placeholder
  text — ceremony never executed (§6); docs/METRICS.md has no sprint-01 row
  (velocity/escaped defects untracked); AGENTS.md says "Tag v0.1.0 at sprint close
  pending" but v0.1.0 exists (stale memory file); spike/bootstrap-01 branch not
  deleted; issue #008's "Epic: E5 (QA)" — epics.md E5 is "Lighting & time", no QA epic.
  Also: docs/PARITY.md forward-references "#013 lighting" but no issues/013 existed —
  this audit now owns #010–#018; the lighting forward-ref must be renumbered (#015.7).
  → P2-2.

## Task 6 — REFERENCE honesty

- Gold ore: line 39 is a [TBC] mess ending "Decision: treat gold_ore as dropping
  `gold_ingot` (as pre-raw-era) [TBC]". Shipped registry drops `gold_ore` and counts
  the block. Behavior was implemented from an explicitly unresolved spec (DoR rule:
  "[TBC] remains → cannot be READY"). Registry value happens to be likely-correct for
  1.12.2, but the [TBC] leaked and REFERENCE contradicts the shipped game. P1-2.
- REFERENCE "wrong tier that can break = ×5" (line 235) is stated as fact, no [TBC],
  and is WRONG for 1.12.2 (canHarvest divides by 30 vs 100 → ×10/3). The clone's
  11.25s stone-by-hand inherits the doc's guess. Guessy mechanic = §1.4/§2 violation.
  P1-1 (doc + code + assert all share the bug).
- REFERENCE step-up "auto step 0.6" vs player.js 0.55 — implementation diverged. P2-4.
- REFERENCE snow layer (78, 8 metas, silk-only drop) vs catalog "snow v=1, id 80,
  'snow block'" — catalog/REFERENCE/implementation disagree; 1.12.2 never had a snow
  cube. P1-3.
- Gravel "flint 10% [TBC %]" and leaves sapling/apple chance: registry ships
  deterministic simplifications while counted. P1-2/P1-4.
- Correct-in-1.12 checks: stone→cobblestone, grass→dirt, coal→coal, diamond→diamond,
  iron_ore→itself (minTier stone), gold minTier iron, obsidian minTier diamond,
  glass drop:null, bedrock unbreakable — all match 1.12.2 rules. Good.

## Findings (severity per §9; P0 none)

| ID | Sev | One-liner |
|----|-----|-----------|
| F1 | P1 | Wrong-tool break multiplier ×5 (REFERENCE+interact.js+assert) vs 1.12.2 ×10/3; stone-by-hand 11.25s not 7.5s; issue #006 AC2 text contradicts its own evidence → issue 010 |
| F2 | P1 | Gold-ore drop implemented from a [TBC] spec (DoR violation); REFERENCE "gold_ingot [TBC]" contradicts shipped "gold_ore"; gravel/leaves drop [TBC]s also shipped as simplifications → issue 011 |
| F3 | P1 | Counted tier-2 "snow" is a 1.13-era snow block; 1.12.2 has only snow_layer (8 metas, silk-only drop) → parity inflated 16→true 15, catalog+worldgen affected → issue 012 |
| F4 | P1 | §7 "behavior implemented" unmet for counted grass/sand/gravel/leaves (spread, gravity, flint, decay/sapling) → issue 013 |
| F5 | P2 | Parity functional flag is self-declared: 1-word edit to built file counts bedrock (reproduced: 17/399); no per-variant evidence binding → issue 014 |
| F6 | P2 | Scrum debt: sprint-01 Review/Retro placeholders unfilled, METRICS has no sprint-01 row, AGENTS.md stale tag line, spike branch not deleted, #006/#008/#009 3-in-1 merge, #008 wrong Epic → issue 015 |
| F7 | P2 | Issue #009 closed with none of the §3 sections; F3 [x] backed by existence/toggle only, content vs REFERENCE unchecked → issue 016 |
| F8 | P2 | Step-up height 0.55 in code vs REFERENCE 0.6 (silent divergence) → issue 017 |
| F9 | P2 | "[x] AABB collision" has no horizontal wall-collision/step-up assert; collision proven only for vertical landing → issue 018 |
| F10 | P2 | Nits (no issue): render AC4 threshold 6s vs AC text <2s (measured 700ms, passes anyway); grass evidence sheet low-contrast (already #007 nit) |

Positives (recorded): offline enforcement real; deterministic build/shot/atlas regen;
bedrock + missing-tile families honestly excluded; 399-enumeration discrepancy openly
documented; textures genuinely procedural with grain; asserts are behavior-level,
not vacuous; every merge --no-ff + conventional.

## Verdict

Gate GREEN and reproducible; evidence trail is largely real and honestly produced —
but the headline metric is inflated by 1 (snow, F3) and 4 more counts over-claim §7
behavior (F4); one wrong 1.12.2 mechanic was baked into doc+code+test together (F1),
and a [TBC] spec leaked into a shipped, counted block (F2). P1 findings block new
sprint work per §9 until 010–013 are planned; P2s go to backlog.

Next-audit re-checks: parity numbers after 010–013 land (expect honest restatement),
014 negative-control assert exists, sprints/01.md review+retro filled, METRICS row.
