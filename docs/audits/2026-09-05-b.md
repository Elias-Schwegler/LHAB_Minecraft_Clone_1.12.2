# Audit #5 — scoped (sprint-03 it7/it8 cluster) — 2026-09-05

Auditor: separate adversarial session. Commit audited: **a6d1c26** (head of main).
Scope: 3cbf487 (PLAYBOOK+AGENTS slim) · be32c08 (#033 suite split) · 22fc63d (#035 mob core) + merge a6d1c26.
Working tree clean before and after all runs (`git status --porcelain` empty). No source modified by auditor.

## 1. Gate re-run (verbatim)

- `node tools/build.mjs` → `build OK -> game/index.html (175.6 KB, atlas embedded)` exit 0.
- `node tools/test.mjs --quick` → `TEST GREEN (spec: quick)` — suites boot/world/light/render/player/
  interact/f3/items/ui/survival/mobs/save — **asserts: 114 pass / 0 fail**.
- `node tools/test.mjs` (full) → `TEST GREEN (spec: all)` — 15 suites incl. slow grass/time/fluids —
  **asserts: 125 pass / 0 fail**, registry blocks: 22, 0 console errors.

Full-count claim (125, 0 errors) TRUE. **Quick-count claim (96) FALSE — measured 114** → F1.

## 2. Suite-split honesty (#033)

- Built-file `CF.assert(r,'x...'` call-site regex: **125 sites**, exactly matching the 125 full-run
  passes (every site executes once). Family tally: boot1 gl1 loop1 registry5 atlas1 world13 light7
  time6 fluids5 render8 player6 interact11 f33 items11 ui8 surv10 mob18 save7 torch3.
- Slow-tier math: grass(1)+time(5)+fluids(5)=11; 125−11=114 quick. At be32c08 full was 107 →
  quick 96 — so the *historic* 107==107 parity claim is arithmetically consistent; only the
  *current* quick number is stale (F1).
- Families present and green: light.* 7/7, time.* 6 (5 slow-suite + time.pixel-night in render),
  fluids.* 5. **No `grass.` prefix exists**: the grass suite's single assert is `world.grass-spread`
  (src/world.js:516) — no doc claims a grass.* name, so this is a naming nit, not a false claim → F3.
- All 18 mob asserts present in built file: land, no-sink, wall-block, spawn-scan-found, spawn-dark,
  no-spawn-noon, no-spawn-lit, min-dist(x2), spawn-night, cap, burn-setup, burn-day, no-burn-night,
  drop, env-kill-no-loot, px-draw, glErr.

## 3. #035 spec claim — 1.12 spawn light <= 7 (not 0)

- Gate code src/mobs.js:40-41: `eff = max(blk, sky*dayFactor); if (eff > 7) return null;` — matches
  1.12 (and REFERENCE flags "light 0" as the 1.18+ trap). Sprint-03 table row corrected in-merge ✔.
- Asserts genuinely exercise BOTH directions:
  - accept (<=7): scan column requires blk<8 at midnight (eff=blk) → `mob.spawn-dark` + scheduler
    `mob.spawn-night` — src/mobs.js:228-235, 251-255.
  - reject (>7): noon open surface sky15*1=15 → `mob.no-spawn-noon` (:237-239); glowstone-adjacent
    with light loop guarded until blk>=8 → `mob.no-spawn-lit` (:241-244). Self-guarding: the lit
    assert can only pass if rejection is caused by blk>7 (all other gates empty/cleared).
- Burn/distance/cap/drop claims match code (dayFactor>0.6+sky>=10, 24-block dist :43, cap 12 :34,
  player-kill-gives-loot / env-kill-no-loot :58). Deviations (a)(b)(c) all carved to #044. **PASS.**

## 4. Parity + evidence literals

- `node tools/parity.mjs` → **17/399 (4.3%), t1 17/125, t2 0/163, t3 0/111** — identical to the
  re-synced PARITY.md table (the "drifted at 14" correction in 22fc63d is the live truth). qa/parity-latest.json matches.
- Newly [x]'ed mechanic rows — every cited assert literal exists in game/index.html:
  hotbar/inventory row: ui.open ✔ ui.drag ✔ ui.result ✔ ui.craft-collect ✔ (8 ui.* total);
  torches+spawn row: torch.on-wall ✔ torch.no-float ✔ torch.pop ✔ + mob.spawn-dark/night/scan-found ✔;
  tools-tiers row: items.iron-pickaxe ✔ items.speed-stone-wood ✔; survival row: 10 surv.* ✔.
  (Also verified for the still-[ ] crafting row's citation: items.smelt ✔ items.planks/table/torch ✔.)
- Cited screenshots exist: ui-inventory.png, tower-save.png, fluid-pool/lava, night-world/glow,
  walking (qa/2026-09-04 + baseline), hud-low.png, night-mobs.png (qa/2026-09-05). All 17 counted
  variants have qa/blocks sheets (mechanically enforced by parity.mjs anyway).

## 5. Vision — qa/2026-09-05/night-mobs.png

VIEWED. Two box-humanoids present mid-frame (green head cube + blue-grey body column, one near
center ~x370, one behind ~x490), standing on dark plains with tree silhouettes; brightness equals
scene night level (dim, not full-bright); NO magenta; hotbar/terrain unaffected. #035 AC7 verdict
"two blocky humanoids... not full-bright... no magenta" = **HONEST PASS**.

## 6. Regression / baselines

- starter-world.png blob hash `23950e4` identical at be32c08^, HEAD, and worktree.
- `git status --porcelain -- qa/baseline` empty; `git diff be32c08^ HEAD -- qa/baseline` empty.
- 22fc63d touched render.js ONLY for mob VAO/pass wiring (vertexAttribPointer block); no shader
  source, no atlas change — "same shader, atlas untouched" claim verified. Full-suite 107→125 with
  zero existing asserts altered in outcome: no regression.

## 7. Issue hygiene

- issues/035: "125 pass / 0 fail", "+18 mob asserts" — both TRUE this session. BUT "Quick gate
  unchanged 96 (mobs suite ... runs in the 32s loop)" is FALSE and self-contradictory: the mobs
  suite DOES run in quick → 96+18=114. Same false number shipped in AGENTS.md gate line → F1.
- issues/044 exists as declared carve-out (DRAFT, correct scope) ✔. Sprint-03 tracker: 035 row
  DONE + spec-correction annotation, it8 daily line ✔, coherent with AGENTS merge list ✔.
- PLAYBOOK.md (3cbf487) §2 still says "expect ... 107 asserts" (true at its own commit, stale now)
  and "14 suites" vs 15 reported names → F2.

## Findings (severity per MASTERPROMPT §9; P0/P1 none → no FIX issue opened)

| ID | Sev | One-liner |
|----|-----|-----------|
| F1 | P2 | Quick-gate assert count misclaimed 96 ("unchanged") in issues/035 Evidence + AGENTS.md; measured 114 (mobs is quick-tagged: 96+18). Claim contradicts its own parenthetical; violates run-before-claim honesty rule. Doc-number fix at next merge; gate itself green both modes. |
| F2 | P3 | PLAYBOOK §2 stale-at-a6d1c26: "expect 107 asserts" (now 125), "14 suites" vs 15 printed. Update ritual doesn't list PLAYBOOK — consider adding numbers line or removing the count. |
| F3 | P3 | grass slow-suite asserts under `world.` prefix (world.grass-spread) not `grass.*` — suite/prefix mapping inconsistent with SUITES naming; harmless (parity math intact: 125=114+11) but future count-by-family greps will miss it. |

## Verdict

**READY.** Both gates independently reproduced GREEN (quick 114/0, full 125/0/0-errors); #033 split
is lossless; #035 spec correction, asserts, parity 17/399, evidence literals, screenshots, vision
verdict and baselines all verify against artifacts. One P2 (false quick-count in docs — correct at
next merge; no functional or parity misrepresentation) and two P3 nits recorded.
