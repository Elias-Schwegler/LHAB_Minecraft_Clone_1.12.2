# Audit #4 — sprint 02 close — 2026-09-05

Auditor: separate adversarial session. Commit audited: c524b4e (head of main).
Working tree clean before and after all gate runs (`git status --short` empty).
Closed findings NOT re-litigated (#010-#019, #029, #030-F1 content, #031 carve,
audit #3 F3 items-count, audit #3 F5 gold-tool, audit #3 F6). Audit #3 "re-check"
items verified at the end.

## Final gate (reproduced verbatim)

`node tools/build.mjs` → `build OK -> game/index.html (156.5 KB, atlas embedded)` exit 0.
`node tools/test.mjs` → `TEST GREEN`, **108 asserts pass, 0 fail, 0 console errors**,
`  ticks: 42, registry blocks: 22`.
NOTE: the loop expected **107** (AGENTS.md "exact 104 asserts"→"107"; prompt said 107) —
true count is **108**. One extra assert landed after the count was recorded (#028 added
torch.pop after the 107 snapshot). Not a gate failure; count-memory drift → F3.

## Parity line (parity.mjs)

```
PARITY @ 2026-09-05 — functional variants 17/399 (4.3%)
  tier 1: 17/125   tier 2: 0/163   tier 3: 0/111
```

## Task 4 — independent metric recount (no parity.mjs code used)

Own parser (node -e): extracted REGISTRY markers from **game/index.html** (built artifact),
parsed docs/catalog.json (184 entries), tools/tex/atlas.json, fs-checked qa/blocks sheets.
**INDEP COUNT 17/399 — identical.** Counted set:
stone, grass, dirt, cobblestone, planks-oak, log-oak, leaves-oak, sand-sand, gravel,
gold_ore, iron_ore, coal_ore, glass, obsidian, torch, diamond_ore, glowstone.
Zero anomalies: no functional flag without green-able proof.tests literals, without
blender-sourced tiles, or without a qa/blocks sheet (parser prints FLAG-NO-* — none printed).
Built-artifact truth-checks: `water` and `lava` variants `functional:false` (hardness -1,
minTier 9, liquid tag), `furnace`/`crafting_table` `functional:false`. Honestly NOT counted ✔.

## Task 2 — sampled sprint-02 closes

- **#021 day/night — PASS.** 6 time asserts real (endpoints+monotone+48k-tick+GPU A/B,
  verified in audit #3 and still green: time.pixel-night 69<157). Audit #3 F2 fixed:
  `light.cave-sky0(0)` now present AND non-vacuous (carves a 1-cell air pocket sealed in a
  3x3x6 stone shell, asserts sky channel === 0 — src/world.js:434-443). Vision re-viewed:
  **night-glow.png** — black sky, dim moonlit grass, warm glowstone block + local halo at
  tree base. Claim holds. ✔
- **#022 fluids — PASS (mechanics scope; honest carve).** 5 fluids.* asserts green
  (spread caps at 7, obsidian, solidify, lava→water stone, budget). Interaction code
  (src/world.js:276-306) is Java-1.12-accurate incl. the asymmetry: water→lava-source=obsidian,
  water→flowing-lava=cobble, lava→water(any level)=stone, and the cobble-shield nuance is
  documented in REFERENCE.md fluids section ("verified" Java rule + impl-verified nuance).
  Vision viewed: **fluid-pool.png** — terraced translucent blue pool in carved basin, hotbar
  intact; alternating dark surface stripes VISIBLE, which is exactly the artifact disclosed
  and carved to #031 (issue status READY, all ACs unchecked — honest). Water/lava uncounted
  pending buckets ✔. P3s: AC2 wording stale (F4), lava speed spec unimplemented (F6).
- **#024 items/crafting — PASS.** 11 items.* asserts green (evidence says "13" — already
  recorded audit #3 F3, not re-litigated). Recipes/break-times already parity-verified in
  audit #3. **torch-craft.png** viewed: night carved room, cross torch with lit head, radial
  light pool with distance falloff to dark corners — hero-shot claim holds. Furnace/crafting
  honest-not-counted statement true in built artifact. ✔
- **#025 inventory/hotbar UI — PARTIAL → issue 032.** 8 ui.* asserts, exactly as claimed,
  real (DOM count, live atlas icon pixel, digit-select, drag transfer, craft result, collect,
  close-returns). But **AC4 "furnace UI: smelt progress advances (shot)" is checked [x] and
  is false**: zero furnace references in src/ui.js; no right-click handler, no 3-slot panel,
  no progress UI, no shot PNG. Only the furnace *logic* exists (items.js furnaceTick).
  AGENTS said "furnace/GUI -> #025"; the deferral target closed without shipping it → F1 P1,
  issue 032 (AC4 unchecked in this commit as the integrity half of the fix).
- **#026 survival — PASS.** 10 surv.* asserts (count exact). **hud-low.png** viewed: hearts
  row (3 red pips + gray) and food row (3 orange + gray) above the 9-slot hotbar, legible at
  854x480 — matches "3.5/10 / 3/10" modulo square-pip half-heart being visual-only (deviation
  documented in issue). P3: AC1 "= 7 dmg" vs `Math.floor(dist-3)` — observed 6 dmg @ f=9.92
  inside the documented 11..14 hp band; Java ceil() would give 7 (F5).
- **#028 torch attachment — PASS.** torch.on-wall / no-float / pop(g=0, c=21/21) green;
  per-face attachment codes 1/2/4/6/8 claimed in evidence and literally implemented in the
  per-face SUPV table at src/world.js:101 (support-direction lookup on removal, drop+give).
  The "fidelity win found by the test" claim matches the code shape. ✔

## Task 3 — honesty scan

- 17/17 functional:true flags proof-bound; every proof.test literal present in the built
  file (independent check, no misses).
- water/lava/furnace/crafting_table functional:false — nothing counted that shouldn't be.
- PARITY.md mechanic checklist: every [x] row maps to real green asserts + existing shots
  (place/break #005/#006, day/night+light #020/#021, fluids #022 mechanics w/ honest
  "buckets->#031" note, health/hunger #026, save #023, F3 #009). **No unverified [x]** there.
- Stale-under-claim rows (opposite direction, still memory drift): hotbar [ ] despite #025,
  crafting/smelting [ ] despite #024/#025, items/tools "pending" despite #024; header table
  frozen at 14/399 while labeled "live from parity.mjs" → F7 P3.
- spike/ scratch confined; no [TBC] in any src/ file (grep zero).

## Findings (severity per §9; P0 none)

| ID | Sev | One-liner |
|----|-----|-----------|
| F1 | P1 | #025 AC4 (furnace UI + shot) false [x]: no furnace GUI in ui.js/harness/shots; deferral target of #024 closed without it → issue 032; AC4 unchecked in this audit commit |
| F2 | P2 | #030 closed DONE with all AC boxes left `[ ]` and no AC2 evidence (the "sibling sheet spot-check pass list" audit #3 asked for is absent; evidence covers AC1 only). I viewed 4 counted-block sheets (torch incl.) — all render fine — but the required list was never written |
| F3 | P3 | Assert-count drift again: AGENTS/prompt say 107, true count 108 (torch.pop landed late); AGENTS.md also still says "Block%: 17/399" correctly but the gate memory should track 108 |
| F4 | P3 | #022 AC2 text says "water into lava source -> **cobblestone** assert" — shipped/asserted rule is **obsidian** (spec line and code correct; AC text stale) |
| F5 | P3 | #026 AC1 promises 10-block fall "= 7 dmg"; impl floors (`Math.floor(dist-3)`) and observed 6 dmg — band documented, AC text not |
| F6 | P3 | Lava slowness unimplemented: spec says "flows slower (every 4 ticks)" — fluidStep has no per-fluid lag (lava spreads at water's queue rate; only maxLv 3 differs). REFERENCE tick-interval [TBC] sits on an implemented code path; lava distance-3 has no assert |
| F7 | P3 | PARITY.md human view stale: table frozen at 14/399 under a "live from parity.mjs" header; 3 mechanic rows under-claim ([ ] hotbar/craft/items despite #024/#025 closes) |

## Audit #3 re-check list

- 030 closed w/ regenerated sheet: **sheet ✔** (viewed: cross torch + lit tip on pedestal,
  warm pool, 52 KB) — **sibling pass-list ✘** (F2).
- green light.sky-cave(0) assert: **✔** (light.cave-sky0, non-vacuous carve+seal).
- AGENTS assert count current: **✘** off-by-one (F3).

## Verdict — v0.2.0 tag

**READY with condition:** this audit commit (unchecks #025 AC4, files 032) is merged as part
of the tag ceremony. Block% metric is honest and independently reproduced (17/399), all
sampled evidence claims match reality except the one furnace-GUI checkbox that this commit
removes, and open work (#031 fluids visuals, #032 furnace GUI) is properly tracked with
unchecked ACs. Sprint-03 planning must carry 032 (GUI) + 031 (visuals) and refresh PARITY.md
table/checklist rows (F7) at sprint start.
