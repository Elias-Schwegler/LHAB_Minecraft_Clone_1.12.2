# Sprint 04 — "Open the underworld, close the loop" (planned 2026-09-07)

Goal: first playable DIMENSION (nether via SPK-7 GO), first Tier-2 gameplay (farming), and close the last
two Tier-1 tails (infinite streaming verify, recipe book) - while grinding cheap countable variants.
Themes: variant economy (wood/wool/storage), new block MODEL classes (slab/stairs multi-box), portal+warp+
persist v2, redstone SPK as Tier-2 gate. Exit criterion: player builds a portal, lights it, fights ghast-less
nether (safe skeleton-free area), comes back through the same portal after a SAVE+RELOAD.

## Committed issues (capacity check: ~14 issue-days, it cadence ~1/iteration like sprint 03)
| # | type | title |
|---|------|-------|
| 045 | FEAT | crafting table 3x3 GUI (was sprint-03 backlog pull - carried) |
| 044 | FIX | mob polish bundle (facing, head-torso gap, poison effect, persistence; art items -> #048) |
| 048 | FIX | texture fidelity pass + fluid-pool black patch (F7) + clean fluid sheets |
| 049 | FEAT | wood variants II: birch + jungle (log/planks/leaves/sapling + sapling growth) +8 variants | DONE (+9 actual) |
| 050 | FEAT | wool spectrum: 15 color variants (dye chain later) - asset + registry grind | DONE (+16) |
| 051 | FEAT | storage blocks: gold/iron/diamond (block<->9 items 1.12 shapes) + mossy cobble + brick_block | DONE (mossy -> vines issue) |
| 052 | FEAT | slabs v1: stone/cobblestone/brick (single+double, AABB half, placement face rule) - multi-box model class | DONE (brick slab -> needs brick_block variant; stone+cobble+oak shipped) | |
| 053 | FEAT | stairs v1: oak/stone/cobble (facing meta, 4-step AABB) on the slab model class | DONE (oak53/stone67=COBBLE/brick108; +subRect clamp fix restored ALL box render) |
| 054 | FEAT | farming v1: hoe + farmland + wheat/carrot/potato growth + bread/food - Tier-2 farming begins |
| 055 | FEAT | portal: obsidian frame validate + flint&steel ignite + portal block + CF.warp per SPK-7 + persist v2 (dims map, v1 migration) - THE sprint centerpiece |
| 056 | FEAT | nether gen: makeWorld genOpts (netherrack+quartz+lava seas+no skylight) + bed-explodes rule + 8:1 coords |
| 057 | SPK | redstone v1 spike: signal model + sim budget at scale (GO decides sprint-05 redstone FEAT chain) |
| 058 | FIX | Tier-1 tail: infinite-streaming verify (walk-through shot w/ chunk counts + regression asserts) |
| 059 | FEAT | recipe book UI (known-recipes list, click-to-fill grid; Tier-1 remaining) |
Stretch (pull only if dailies show slack): #060 sign text? no - ladder; snow_layer + ice (+freeze rules).
Explicitly NOT this sprint: ghast/blaze (needs 055+056 shipped first), End, enchanting, villagers, redstone FEATs.

## DoR notes
- 049-053 are variant/model GRIND: gen.py palette params + registry entries + blockshots + asserts; each
  must land its own merge with sheets (pattern proven by #043).
- 052/053 invent the multi-box model class (bed already does 2-cell): decision recorded in issue = one
  generic `model: 'slab|stairs'` + AABB list, not per-block hacks.
- 055/056 MUST follow docs/spikes/SPK-7.md design (reassign CF.world + renderReset + persist v2 dims;
  portal block id reserved from parity.mjs print). SPK-7 risks are their ACs (1.3s swap hidden by portal
  anim frame, v1 save migration tested).
- 057 SPK gates Tier-2 redstone per MASTERPROMPT §5 - no FEAT without GO.

## Sprint-04 planning notes
- Velocity basis: sprint 03 = 14 issues/13 iterations, ~6 asserts/iter, 0 audits P0/P1 at close.
- Tag planned: v0.4.0 after close + audit #7.

## Daily (sprint 04)
 #049 DONE - birch/jungle logs+planks+leaves + sapling blocks (growSapling API, name-based decay,
  20% birch worldgen); atlas grew to GRID12/192px (numpy icon pass - Cycles had flattened icon ALPHA);
  FOUR latent render bugs fixed: NaN liquid/torch UVs (const A shadowed by coord A), torch texture flipped
  since #024, floor faces culled under non-solid neighbors (black holes), water/glass alpha restore.
  PARITY 20->29. 209/193 GREEN.
- it21: #050 DONE - 16-wool spectrum (wool_* ramp tiles, catalog labels fix - families need variant NAMES
  in catalog.json to count!), interact.drop-wool-red, registry.wool-spectrum; PARITY 29->45 (11.3%);
  211/195 GREEN; wool-wall.png vision PASS. #051 DONE - gold/iron/diamond/brick/clay + storage 9<->1
  recipes + clay_ball->brick smelt + dropN plumbing + interact.clay-drop4; PARITY 50/399 (12.5%);
  214/198 GREEN; storage-wall.png vision PASS; mossy deferred (vines). Next: #052 slabs (multi-box model).

- it22: #052 DONE - multi-box model class (mesher pass + coverFace culling + physics cellTopAt + top-face
  upgrade-to-double + slab skylight transparency); 5 latent render/light bugs fixed (chunk-origin box corners!,
  abs-vs-local UVs, boundary culling, face light, transparency); 3 slab variants, PARITY 53/399; 216/200 GREEN;
  slab-scene vision PASS. Next: #053 stairs on the box class.
- it23: URGENT user-report fixes #105+#106 same-day (branch fix/105-106-torch-blackfaces): torch = 3 stacked bugs (cross-pass chunk-local verts -> drawn near origin; sky-column seed killed by non-liquid id AND
  emitter overwrote packed byte -> br 14/255 near-black; UV then verified flame-up + new interact.torch-up pixel arbiter);
  black -faces = lightCell() float-index (plane-0.001) -> undefined -> NaN -> 3 of 6 faces black everywhere - instrumented pushQuad dump cracked it;
  faces-corner scenario never drained the LIGHT queue (stale 0 = its own black skirt); slab skylight passability re-added after bisect exonerated it;
  starter-world/faces-corner/torch-probe/slab-scene all vision PASS - world genuinely lit now; +2 asserts, 6 new PLAYBOOK lessons (incl. vision-rationalization failure); 218/202 GREEN. Next: #053 stairs.
- it24: #053 DONE - stairs on a centralised model resolver. CF.boxesOf(v,flat)+CF.cellOpaque(v,flat) are now THE single source for multi-box models (slab+stairs) used by mesher, physics, placement, and light passability - removes the inline bit-re-derivation in 4 files that caused #105/#106 drift. 1.12-correct mapping: stone_stairs(67)=COBBLE texture (no smooth-stone stairs in 1.12), brick_stairs(108). subRect per-face rect-minus culling. CRITICAL: my box-pass rewrite had a subRect axis-clamp bug (V clamped to U range) that zeroed EVERY box face -> all slabs+stairs flat/black; caught by vision QA re-checking slab-scene. Fixed+re-verified. Also found+fixed 2 latent gaps: (a) CF.place used undeclared p (bed ReferenceError in-game, only reachable via RMB not the direct bedPlace test path); (b) chunk lat meta array never persisted -> slab halves/wall-torch faces/bed halves lost on save+reload (save.flat assert now). physics: box-vs-box collision + footprint-aware landing (+0.55 half-box tolerance). +5 asserts, 224/208 GREEN, parity 53->56/399 (14.0%). stair-run+3 block sheets vision PASS. Next: #054 farming v1.
- it25: #060 DONE (polish sweep from user gap-eval): Q-drop item entities (1.12 throw physics, 0.5s pickup delay, 1.6 magnet, 5-min despawn, 200 cap) drawn as atlas-icon crossed billboards (new dynamic VBO pass, same shader/sampler); pause overlay + full sim freeze on mouse-release or inventory-open (freeCam/scripted/sleeping exempt -> harness-safe); RMB browser-menu suppressed; boot-load now shows the controls overlay. +1 assert (interact.q-drop), 225/209 GREEN; drop-pickup.png vision PASS (itris=12, items rest exactly on floor). ALSO: caught my own #053 regression - an Edit had swallowed cellHitsPlayer's const-p into the trailing comment (every place threw); lesson -> PLAYBOOK. Next: #054 farming.
