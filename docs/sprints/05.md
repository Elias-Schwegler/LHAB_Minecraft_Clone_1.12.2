# Sprint 05 — "Current flows" (planned 2026-09-17)

Goal: first Tier-2 INTERACTION SYSTEM: redstone (SPK-8 GO design), plus the carried polish bundle (#044)
and fidelity pass (#048) that the video-QA law now verifies every iteration. The sprint CLOSE must pass a
full gameplay-video re-evaluation BEFORE audit #8 + tag v0.5.0 (DoD 4b law).
Themes: redstone chain (dust/torch -> repeater -> lamp -> piston-lite), mob polish, texture fidelity,
#061 suite decoupling, spider/enderman roster, nether follow-up seeds.
Exit criterion: player crafts redstone torch from loot, wires a door-open lamp circuit in-game, and the
close video shows it all working + no regression.

## Issues (mirrored same numbering)
| # | type | title |
|---|------|-------|
| 064 | FEAT | redstone dust + redstone torch: power nibble on relight pattern (SPK-8 model A, per-region re-flood), craft recipes, blockshots |
| 065 | FEAT | repeater: 1-4 tick delay queue (fReady pattern), orientation from yaw, signal re-boost; redstone_torch invert note |
| 066 | FEAT | redstone lamp: unlit/lit block pair, powers from any adjacent powered cell, light 15 when lit |
| 067 | FEAT | piston-lite: extend/retract animation (model offset, 1.12 push 12-block cap skipped), sticky out |
| 068 | FEAT | inputs: pressure plate (entity detect reuses cellBoxes) + button (5t timer) - the "wiring glue" |
| 044 | FIX  | mob polish bundle (carried: facing, head-torso art gap, poison effect display, persistence-lite) |
| 048 | FIX  | texture fidelity pass (carried: F5 quartz veins, torch flame art, mob-drop/chain icons, blank-item tiles) |
| 061 | FIX  | suite order-coupling: each suite self-setups (unlocks surgical --suites repro) | DONE (prelude in runTests) |
| 069 | FEAT | spider + enderman roster (sprint-04 pull-forward, #044-adjacent art) |
| 070 | SPK  | nether follow-up scout: ghast fireball + fortress gen costed (SPK-9, decides sprint 06 nether-II) |

Chain order: #064 -> #065 -> #066; #067/#068 after #064. (#062 rig + #063 fix landed pre-sprint.)

Explicitly NOT this sprint: enchanting, potions, villagers, biomes-II, mobs-on-pistons, water physics II, End.

## DoR notes
- 064-068 MUST follow docs/spikes/SPK-8-redstone.md (power = DERIVED nibble, re-flood on set() beside
  queueRelight, NO persistence, region budget; repeater delay = fReady-style due-list scanned in tick).
  Power render: tinted dust lines (gen.py tile family) - functional:false until sheets exist (honest parity).
- Video law every gameplay issue (DoD 4b): record `video.mjs <ticket>` + review frames; sprint close = FULL
  re-evaluation run of 'basic' + the redstone scenario merged into the driver (extend video-play with a
  circuit phase once 066 lands).
- #061 early (it1): cheap, and every later surgical repro test benefits.

## Daily (sprint 05)
- it1: #061 DONE (suite decoupling). harness prelude = ready+GL wait, arena(4)+ground player, loop-stop for
  boot-less selections; save.v2 self-provisions nether. Matrix of 11 singles + combos GREEN; full 242/0.
  NEXT: #064 redstone dust+torch (SPK-8 model A).
- it2: #064 DONE - redstone power core per SPK-8 model A (src/redstone.js: cells+power maps, re-flood ONLY on
  RS change - idle-free verified by rsFloods counter assert; rsRescan after load = zero persistence;
  CF.rsPowerAt API ready). wire/rstorch painted functional:false + redstone_ore BLENDER functional:true ->
  PARITY 59/399. Ore veins y<16, smelt, dust+stick->torch recipe. +7 asserts, 249/233 GREEN, 47 blocks.
  video-redstone PASS (live-loop power d1=14/d5=10). Landmine #3: hotId JSON-id FIELD -> IDOF (mid-JSON inserts
  place-wrong-block!). Torch inverter deferred -> #065. NEXT: #065 repeater (delay queue + orientation + invert).
- it3: #065 DONE - repeater + TORCH INVERTER. Engine now Jacobi fixed-point (cap 6): passes PURE, post-sweep
  schedules rsDue (fReady-style, +2gt on, instant off), DIODE injection output-only (backward-leak bug caught),
  torch inverter via blockPowered(attach=torch+SUPV sign-fixed). Repeater painted tile (functional:false,
  parity stays 59), wiki-exact 3x3 recipe, flat bits = yaw XOR 1 (+1-vs-XOR axis bug caught by LIVE probe).
  +4 asserts -> 254/238 GREEN, 48 blocks; video-redstone extended: torch->dust->repeater->dust, live d5=10
  -> d7=14 BOOST through the real game loop. RMB delay-setting cycle deferred. NEXT: #066 lamp.
- it4: #066 DONE - REDSTONE LAMP = first VISIBLE circuit state. 1.12 id pair redstone_lamp/lit_redstone_lamp,
  post-flood swap sweep (instant on; 2gt off-delay omitted, noted), blockPowered + torch-opposite-attach rule
  (lamp above floor torch lights; inverter invariant intact). world.lamp-on/off + items.lamp-craft (first try
  had corners/cross inverted - assert caught). Video lamp finale: live probe lamp=LIT light=15 + frames show
  glowing lamp at end of torch->dust->repeater->dust. SPRINT EXIT CRITERION MET. 256/240 GREEN, 50 blocks,
  parity honest (functional:false, art -> #048). NEXT: #068 plate+button (input glue) then #067 piston-lite.
- it5: #068 DONE - plate+button INPUT glue. Plates: rsTick entity-overlap scan (player+mobs), strong-power-
  above rule; buttons: RMB chain-head pressButton, until-map expiry (stone 20gt/wood 30gt per CURRENT wiki -
  issue's 10gt stale), re-press refresh, powers own attach (wiki exception to torch rule). 4 painted blocks,
  parity honest. Suite-vs-live geometry catch: suite tp'd player INTO plate cell (no physics) while live
  physics pushed a test player up onto the lamp-over-plate -> scenario rebuilt as plate->dust->lamp row;
  engine was correct both times. +3 asserts 259/243 GREEN, 54 blocks. video ON/OFF frames prove input side;
  f04 headless mesh-lag filed #071. Lit lamp tile brightened (muddy -> amber, #048 item done).
  NEXT: #067 piston-lite (extend/retract animation, push-1, door demo).
- it6: #067 DONE (piston-lite) - actor sweep in flood: powered+can-push moves ONE block (flat bits ride),
  ext bit 4 = bright head plate (greedy mask bit24 bucket); retract keeps block (not-sticky v1); refuses
  immovables; smooth slide CUT from lite scope (needs vertex anim - logged). Alias tiles side/bottom
  (no atlas growth) + 2 painted. isRsDef() single source fixed LATENT load-hole (repeater/lamp/plate/button/
  piston never re-entered cells on load - only wire/torch did). items.piston-craft wiki-exact. Live probe
  cobbleAt14=true ext=16. +2 asserts 261/245 GREEN 55 blocks, parity honest.
  NEXT: #070 SPK-9 (fast, doc) then #044/#048/#069 + CLOSE (video re-eval + audit #8 + v0.5.0).

## Planning basis
- Velocity basis: sprint 04 = 14 issues/14 iters, +39 asserts, 0 auditor P0/P1 (2 user-reported caught, both
  same-day). Carried count is small (2) by design.
- Tag planned: v0.5.0 at close after audit #8 + video re-eval.
