# Issue: 042 — TNT: craft, place, prime, fuse, explosion
- Type: FEAT | Status: DONE | Epic: docs/backlog/epics.md (Explosives) | Sprint: 03
- Depends on: #037 (CF.explode DONE) | Time-box: 1d

## 1.12.2 Reference spec
- Craft (shaped 3x3): 5 gunpowder + 4 sand -> 1 tnt. Place as a block.
- Ignite: RMB with flint & steel (iron ingot + flint) -> becomes a primed entity, **fuse 80 ticks** (4s).
- Detonate: explosion **power 4** (vs creeper 3) -> CF.explode (#037). Chain reaction: a blast primes/destroys
  nearby TNT blocks (they then detonate). Blocks broken drop as items w/ prob 1/power (-> item entities #044,
  so we do destruction only). Bedrock/obsidian resist (blastRes).
- Deviations: no crafting-table 3x3 GUI yet (only 2x2 hand grid -> TNT craftable via CF.tryCraft/recipe test,
  not yet in the survival UI; 3x3 table GUI tracked as a gap -> #045), no water-adjacent block-drop suppression,
  no minecart TNT ignition, flint&steel durability ignored, primed TNT has no gravity/bounce (fixed at cell centre).

## Acceptance criteria
- [x] AC1 tnt + flint&steel recipes correct (3x3 / 2-3 patterns) -> tnt.craft, tnt.flintsteel
- [x] AC2 place tnt block, RMB flint&steel -> primed entity (block removed, fuse starts) -> tnt.prime
- [x] AC3 fuse counts down then explodes with a power-4 crater (multiple stone removed) -> tnt.boom
- [x] AC4 chain reaction (adjacent tnt doesn't survive intact) -> tnt.chain
- [x] AC5 blast resistance honoured (bedrock survives a blast on top of it) -> tnt.bedrock-survives
- [x] AC6 TNT block renders recognisably (red + band + grey top, procedural atlas cell, NO magenta) -> tnt-fuse.png
- [x] AC7 zero regression: full 166 -> 172 green (+6), 0 errors; parity STAYS 18/399 (TNT functional:false - not Blender-sourced, honest)

## Evidence (closed 2026-09-06)
- Gate: full **172/172 GREEN** (new suite `tnt` +5 asserts: craft/flintsteel/prime/boom/chain/bedrock-survives
  ... plus registry blocks 22->23), 0 errors; parity unchanged **18/399** (TNT kept functional:false because its
  tile is generated in-code, not Blender -> parity.mjs only counts blender tiles; honest, not a hidden skip).
- New src/tnt.js: CF.tnts primed list, primeTNT, tntTick (fuse + power-4 CF.explode + chain scan), useFlintSteel
  (RMB ignition gated in interact before place), __TEXMETA tnt_side/tnt_top registration, tnt-fuse shot.
- src/explode.js blast map already had tnt:0 (destructible) + bedrock/obsidian 1200/720 -> chain + resist work.
- render.js: procedural tnt_side + tnt_top tiles drawn into FREE atlas cells (32,48)/(48,48) at init (existing
  Blender tiles + their UVs untouched => block parity safe); palette slot 15 = TNT red for the primed entity
  (flashes white when fuse<20). mobs.js buildMobVerts renders CF.tnts (swell + flash). game.js loop calls
  CF.tntTick; persist clears tnts on load; manifest adds tnt.js; items.js adds flint_and_steel + 2 recipes.
- Shots qa/2026-09-06/tnt-fuse.png (synthetic stone platform, daytime). VISION PASS: a red TNT block with a dark
  mid band + grey gunpowder top, unmistakably TNT, distinct from the grey stone floor; primed entity + neighbours
  present; no magenta anywhere.
- Tests caught: primed-flash parity is nondeterministic across the live loop (documented; shot forces odd-tick
  frame). Chain vs pure-destruction hard to isolate at 2-block gap (both tnt gone either way) -> assert is the
  honest "second block no longer exists intact".
- Follow-ups: #045 crafting-table 3x3 GUI (unlocks in-UI tnt/flint recipes); #044 TNT item drops via entities,
  primed gravity, charged-creeper-style extras.

> RETRO (#043 P1 audit note, audit #6 F2/F3): the (32,48)/(48,48) cells cited above were NOT free (item iron/gold ingot tiles per the pre-fix icon-stride bug); relocated to (32,96)/(48,96) + atlas regenerated. Also count correction: the tnt suite added +6 asserts, not +5 (166->172 delta).
